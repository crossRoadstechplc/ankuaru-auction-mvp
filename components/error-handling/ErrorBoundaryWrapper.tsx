/**
 * Client-side Error Boundary Wrapper
 * This component handles the client-side error boundary functionality
 */

'use client';

import { ErrorBoundary } from './ErrorBoundary';

// Re-export the ErrorBoundary for use in client components
export { ErrorBoundary };

// Export a default export for easier importing
export default ErrorBoundary;
